.onLoad <- function(libname, pkgname) {
require(shiny)
addResourcePath('roow', system.file('www', package='roow'))
#addResourcePath('roow/images', system.file('www/images', package='roow'))
}
.onAttach <- function(libname, pkgname) {
require(shiny)
}
library(devtools)
install_github('roow', 'oow') # install the package with the datetimeInput widget
library("roow", lib.loc="~/R/win-library/3.1")
